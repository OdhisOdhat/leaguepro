
version: '3.8'
services:
  backend:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - league_data:/app/data
    environment:
      - PORT=3001
      - NODE_ENV=production

volumes:
  league_data:
